scripted_gui_ctp_tier = {
    scope = character
    effect = {
        save_scope_as = effect_root
        custom_description_no_bullet = {
            text = scripted_gui_trigger_ctp_tier
            subject = scope:effect_root
			object = scope:effect_root
        }
    }
}